// import { useGetCartItemsQuery } from "../api/soapApi";

// const updateCartCount = (userId) => {
//   const { data } = useGetCartItemsQuery(userId);
//   return data ? data.cartItems.length : 0;
// };

// export default updateCartCount;
